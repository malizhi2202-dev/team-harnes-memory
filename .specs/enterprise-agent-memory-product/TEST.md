# TEST: Enterprise Agent Memory 产品文档与 HTML 原型

- **Change ID**: `enterprise-agent-memory-product`
- **依据**: `REQUIREMENT.md`（AC-1~12）、`DESIGN.md`、`TASK.md`、`VALIDATION.md`
- **项目类型**: 纯文档 + 离线只读原型（零运行时、零依赖、零网络）——5 轮金字塔按此裁剪，跳过均给理由（R5.4）

## 本次测试范围声明

| 轮次 | 状态 | 范围 | 跳过/裁剪理由 |
|---|---|---|---|
| 第 1 轮 · 功能 | ✅ 必跑 | AC-1~12 全覆盖矩阵（UAT + 机判） | — |
| 第 2 轮 · 性能 | ⚠️ 部分 | 体积预算 + 渲染无回归 + 打印页数基线 | 静态文档无 LCP/INP 预算；以可量化代理指标代跑 |
| 第 3 轮 · 安全 | ⚠️ 部分 | 秘钥扫描 + 依赖扫描 + 攻击面声明 | 零依赖 → `npm audit` 不适用；无服务端 → SAST 不适用；OWASP 逐项标注见下 |
| 第 4 轮 · 兼容 | ⚠️ 部分 | Chromium×5 视口实测 + 主题/打印/UTF-8 | Firefox/Safari 本环境无二进制，登记待人工；无 schema → 迁移测试不适用 |
| 第 5 轮 · 可观测 | ❌ 跳过 | — | 原型不发送任何埋点/日志/网络请求（REQUIREMENT 非功能已声明"可观测性：原型不发送埋点"），无运行时可观测对象 |

## 第 1 轮 · 功能

### 1.1 AC 覆盖矩阵（用例从 AC 派生 · R5.1）

| AC | 类型 | 用例 / 编号 | 状态 |
|---|---|---|---|
| AC-1 工作现场连续性 | manual | UAT-01/02 + 原型 `p2–p5` | 🟡 机判绿，人工目测待做 |
| AC-2 记忆与知识分流 | manual | UAT-02 + `p6` 详情 | 🟡 同上 |
| AC-3 权限与执行门禁 | manual | UAT-08 + `p9` + 三处矩阵一致性（VALIDATION §4） | ✅ 机判绿（矩阵逐格一致）/ 人工待走查 |
| AC-4 清理与外部副本证据 | manual | UAT-08 + `p10` 六态表 | 🟡 机判绿（73 处 blocked 带三要素） |
| AC-5 报告与通知版本 | manual | `p11` 投影表 | 🟡 待人工 |
| AC-6 敏感信息保护 | scripted | 秘钥 grep（见第 3 轮）+ 演示值虚构声明（VALIDATION §8） | ✅ |
| AC-7 状态完整性 | scripted+manual | `p13` 画廊 8 态 + `unknown≠0` 文案 grep | ✅ 机判绿 |
| AC-8 可访问性与离线 | scripted | render-verify 8 项（0 请求/0 JS 错/无溢出/主题持久） | ✅（VALIDATION §2.5/§9） |
| AC-9 生成溯源 | scripted | grep ×3 + `p6` dl 四要素 | ✅ 机判（VALIDATION §9）；措辞有效性 🟡 UAT-09 待人工 |
| AC-10 建议不自我裁决 | scripted | 标注 ×2 在位 + 绕过文案 = **0** | ✅；诱导风险 🟡 UAT-10 |
| AC-11 双时间轴与失效三态 | scripted | 面板 ×2、chip ×1、superseded_by ×4、approved≠published ×15 | ✅；与 §6.10 口径贯通 🟡 UAT-11 |
| AC-12 竞品格局与引用纪律 | scripted | 基准名仅 1 处"不进正文"声明句；伴随分数扫描 0 命中；外链 0 | ✅；定位叙事成立性 🟡 UAT-12 |

覆盖率说明：本项目无可测代码，行覆盖率不适用；以 **AC 覆盖优先**（R5 约束）——12/12 全部有 ≥1 用例，无空缺。

### 1.2 UAT 脚本（增量部分，其余见 VALIDATION §7/§9）

```
UAT-09：分诊溯源可读性
  前置：离线打开 PRODUCT-DESIGN.html，到 #/ingestion/triage（p6）
  步骤：1. 展开 tri-512 详情 2. 顺读「生成溯源」四要素
  期望：- 不查资料即可回答"这条候选是谁、凭什么、什么时候生成的"
  通过/失败：待人工

UAT-10：建议不诱导
  步骤：读「机器合并建议」四行 → 假装自己是评审人做裁决
  期望：- 标注"仅参考"位置醒目；SUPERSEDE 呈现为关系而非"覆盖删除"
  通过/失败：待人工

UAT-11：双时间轴讲得通
  步骤：p7 面板三行（m-9031/m-8807/m-9102）→ 对照 §6.10 清理证据六态
  期望：- 能区分"何时为真"与"我们何时得知"；三态不混用
  通过/失败：待人工

UAT-12：§1.5 定位可信
  步骤：通读对照表 + 三卡片 + 引用纪律 callout
  期望：- 每格能说出证据档位；无基准分数诱惑；缺口不自欺
  通过/失败：待人工
```

### 1.3 边界用例（≥3）与错误路径

- 视口 5 档（1440/1000/900/700/480）横向溢出 = 0 ✅（render-verify 实测）
- 打印媒体：顶栏/目录隐藏 + A4 69 页分页 ✅
- 状态完备性：`p13` 画廊 8 态 + 高风险 blocked 73 处 ✅
- 错误路径：空态/离线/过期/partial 均有专屏表达（继承 CONTRACT 类表）✅
- Unicode：全文 zh-CN + `≠`/`→`/`∪` 符号在 UTF-8 渲染无乱码（Chromium 实测）✅

### 1.4 测试质量自检 · 6 维

- T1 晦涩：机判项全部"命令+期望计数"形式，可直读 ✅
- T2 **脆弱（命中，登记）**：grep 词锚（如 `invalidated ≠ deleted`、`不自我裁决`）在措辞优化时会误红 → 权衡接受：词锚即 CONTRACT 锁定的产品语汇，改词=改语义；已在校对规则中声明。
- T3 重复：AC-9~11 与 §2.5 渲染项无同场景双验 ✅
- T4 mock 滥用：无 mock（静态工件直读）✅
- T5 覆盖率幻觉：所有 ✅ 行附真实计数/0 值，无"仅调用无断言" ✅
- T6 架构错配：文本断言用 grep 而非 e2e；渲染才用浏览器——层级正确 ✅

## 第 2 轮 · 性能（可量化代理指标）

| 指标 | 基线（v0.2） | 现值（v0.3） | 判定 |
|---|---|---|---|
| 单文件体积（预算 cap 800KB，check.sh 强制） | 444 KB | 471 KB | ✅ 59% 预算，+6.1% 与增文幅度相称 |
| 外部请求 | 0 | 0 | ✅ 无退步 |
| Chromium 加载 JS 错误 | 0 | 0 | ✅ |
| 打印页数 | A4 69 页（v0.2 人工实测） | 未复测页数；打印 chrome 隐藏实测 ✅，增文均为流式内容 | ⚠️ 复测并入 UAT-07 人工轮 |

## 第 3 轮 · 安全

- 依赖扫描：**不适用**（零依赖工件；`grep` 证实无 import/require/src URL，check.sh 外链 0）——理由登记。
- 秘钥扫描：`grep -cE 'api[_-]?key\s*[:=]|AKIA|ghp_|sk-[A-Za-z0-9]{20}'` 于 `_build/` + 成品 → 命中为 0；出现的 `sha256:3e7c1d…`、`p-2026.09.2` 均为带标注的合成演示值。✅
- SAST：不适用（无可执行服务端代码；内联 JS 由 check.sh `node --check` 语法级守卫 + render-verify 运行时 0 错误覆盖）。
- OWASP 逐项：A01/A02/A07/A08 ❌不适用（无鉴权无后端）｜A03 ✅（HTML 全静态转义文本，无用户输入通路；emoji/脚本注入由 check.sh 拦截）｜A04 ✅（"建议不自我裁决"即反不安全设计的产品级护栏）｜A05 ⚠️不适用｜A06 ✅（零依赖）｜A09 →第5轮（显式不适用）｜A10 ❌不适用（零网络）。

## 第 4 轮 · 兼容

- Chromium（ms-playwright 缓存版）× 1440/1000/900/700/480：8 项全绿（VALIDATION §9）✅
- Firefox/Safari：⚠️ 本环境无浏览器二进制 → **登记待人工**（与 v0.2 遗留同一敞口，非新增回归）
- 主题 light/dark + localStorage 持久化：✅ 实测；打印媒体：✅ 实测；UTF-8/zh-CN：✅
- 数据迁移/schema 兼容：不适用（本 change 无存储层，Schema 均留 `待确认`）

## 第 5 轮 · 可观测

❌ 跳过——唯一运行时行为是本地 localStorage 主题切换（已实测）；无日志/指标/告警对象。若真实实现 change 立项，本轮在那一侧重跑。

## 回归测试登记（本轮新增）

- 词锚 grep ×6（生成溯源/不自我裁决/invalidated ≠ deleted/superseded_by/时间线与失效/approved ≠ published）
- 绕过文案负模式 grep（自动应用|自动合并落位|无需人工确认 = 0）
- 基准分数伴随模式负 grep（类目名 + 40 字符内数字 = 0 命中）
- check.sh 既有 23 项守卫（含 47 锚点有序、12 侧栏一致、外链 0、≤800KB）
- render-verify 8 项（既有，复跑无退步）

## 反对/待补意见记录

测试门 4/4 通过，保留意见与待补敞口（均登记、不掩蔽）：

1. **跨浏览器矩阵**：Firefox/Safari 本环境无二进制，仅 Chromium 实测——沿承 v0.2 遗留，人工轮补。
2. **WCAG 对比度工具实测**未跑（checklist 级静态保证 + 视觉模型复核替代）——同上遗留。
3. **PDF 分页复测**并入 UAT-07 人工轮。
4. **T2 词锚脆弱**为有意权衡：锚定词即产品语义语汇（CONTRACT 锁定），改词=改语义，误红即报警符合预期。
5. UAT-09~12 的**判断性部分**（措辞支撑裁决、建议不诱导、口径读得通、定位成立）无法机判，明确等待人工过目——TEST 阶段不代签。
