# ROADMAP — memory-systems-fusion

审核: 待审（第1轮）——**本轮无过审结论可并入**（R16.3）。本文件当前只承载两类内容：① 既有原型体系的技术约束（按调度要求预先记录，属事实非结论）；② 待 ⑤ 人审晋级后的下游槽位（空置占位，禁止预填）。

## 1. 既有原型体系技术约束（下游任何改动必须遵守的事实）

来源：`.specs/enterprise-agent-memory-product/_build/CONTRACT.md` 与 `_build/` 目录（本 change 全程未触碰，仅登记约束）。

1. **PRODUCT-DESIGN.html 是生成物**：446KB 单文件由 `_build/build.sh` 拼装；**禁止直接编辑 PRODUCT-DESIGN.html**。内容改动只发生在 `_build/secN.html`（各节分段）、`sec6-open.html`（界面屏）、`override.css`（样式覆写），随后 `bash _build/build.sh && bash _build/check.sh` **双绿**才算完成（check.sh 为机器闸）。
2. **CONTRACT 红线**（改动候选屏/节时逐条对照）：零外部依赖（无 CDN/字体/JS 库）；无 emoji；无图片（纯 CSS/HTML 绘形）；全中文文案；演示数据统一数据集（所有屏共用同一套人/空间/记忆名，改一处须全表同步）；单一事实源（S0–S10 事实表；侧栏 19 路由为唯一真源，新增屏必须同步路由表且三处一致）；8 角色与 16×8 权限矩阵在 §7/§5.0/p12 三处逐字一致；「演示数据」标注不得移除。
3. **上游门禁状态**：product change 处于 T-04 完成、`VALIDATION.md` §7 人工浏览器 UAT（UAT-01/02/03/05/06/07/08）待执行；§8 有四组口径冲突未裁决（Team↔Project 基数、L0/L3 分层、ChangeSet 定位、R/S 与 E 映射）。**融合改动不得先于口径裁决落进冲突面**（D-1 已登记为前置）。
4. **十议题留白纪律**：platform 议题把 Schema/阈值/算法/SLA 系统性留白（76 轮共识）。本 change 的所有「字段/端点」建议都停在语义级；任何要写死数值/格式的冲动 → 回平台议题补录轮（且需人审背书）。
5. **提交纪律**（R4.4/R10）：工件按阶段落盘后一次性提交；文档仓无测试可跑，验证=markdown 链接/路径存在性自检（本 change 已做：GAPS 指针抽查 4 处命中）。

## 2. 下游槽位（待 ⑤ 人审晋级后逐条填入，当前全空）

| 槽位 | 拟去向 | 状态 |
|---|---|---|
| （晋级项将按「1-requirement 增量 / 2a-ui-design / backlog 映射 / 平台议题补录 / 留档观察」分类填入） | — | 空置 |

## 3. 跨议题依赖备忘（登记事实，不构成结论）

- D-2 ↔ D-4：p8「最近被哪些 Run 消费」列依赖血缘反查端点先行。
- D-3 ↔ backlog B13：注入快照屏是 B13（P0）的界面承接件。
- D-5 ↔ backlog C4/C5、D6 金集：判定器是 C4/C5 承接件；主体→资源集清单是金集用例输入。
- D-6 全域：不改 9 步协议编号，只加语义补录，避免与 platform 议题 10 已确认文本冲突。
