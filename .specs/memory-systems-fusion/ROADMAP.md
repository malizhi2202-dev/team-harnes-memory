# ROADMAP — memory-systems-fusion

审核: 待审（第1轮）——**GD 6/6 已过审；⑤ 人审晋级前，过审结论仍不得预填入 §2**（R16.3）。本文件承载：① 既有原型体系技术约束（事实非结论）；② 待 ⑤ 晋级后的下游槽位（空置占位）。§1.6–1.10 为 GD-1 前端架构师「共同约束」并入件（其要求「原样并入 §1 后再数票」，按其口径落实）＋GD-1 高级产品经理横切呈现纪律；§1.11 为 GD-3 产品横切第二条（生成物行号只作辅证）。

## 1. 既有原型体系技术约束（下游任何改动必须遵守的事实）

来源：`.specs/enterprise-agent-memory-product/_build/CONTRACT.md` 与 `_build/` 目录（本 change 全程未触碰，仅登记约束）。

1. **PRODUCT-DESIGN.html 是生成物**：446KB 单文件由 `_build/build.sh` 拼装；**禁止直接编辑 PRODUCT-DESIGN.html**。内容改动只发生在 `_build/secN.html`（各节分段）、`sec6-open.html`（界面屏）、`override.css`（样式覆写），随后 `bash _build/build.sh && bash _build/check.sh` **双绿**才算完成（check.sh 为机器闸）。
2. **CONTRACT 红线**（改动候选屏/节时逐条对照）：零外部依赖（无 CDN/字体/JS 库）；无 emoji；无图片（纯 CSS/HTML 绘形）；全中文文案；演示数据统一数据集（所有屏共用同一套人/空间/记忆名，改一处须全表同步）；单一事实源（S0–S10 事实表；侧栏 19 路由为唯一真源，新增屏必须同步路由表且三处一致）；8 角色与 16×8 权限矩阵在 §7/§5.0/p12 三处逐字一致；「演示数据」标注不得移除。
3. **上游门禁状态**：product change 处于 T-04 完成、`VALIDATION.md` §7 人工浏览器 UAT（UAT-01/02/03/05/06/07/08）待执行；§8 有四组口径冲突未裁决（Team↔Project 基数、L0/L3 分层、ChangeSet 定位、R/S 与 E 映射）。**融合改动不得先于口径裁决落进冲突面**（注：§8 四组不含时间语义——GD-1 领域专家复核，`VALIDATION.md:185-193`；双时态项真约束＝议题 07 词表＋§10.1「不冻结」声明，可与 §8 裁决并行）。
4. **十议题留白纪律**：platform 议题把 Schema/阈值/算法/SLA 系统性留白（76 轮共识）。本 change 的所有「字段/端点」建议都停在语义级；任何要写死数值/格式的冲动 → 回平台议题补录轮（且需人审背书）。**JSON 字段名与形状不进 PRODUCT-DESIGN**（§3.5:1233 声明每行是候选契约）：access/check、逐条证据、消费闭包等一律语义级，命名交 2-design（GD-1 前端架构师）。
5. **提交纪律**（R4.4/R10）：工件按阶段落盘后一次性提交；文档仓无测试可跑，验证=markdown 链接/路径存在性自检（本 change 已做两轮：初稿指针抽查＋GD-1 三票全量勘误回源）。
6. **check.sh 三处隐藏硬编码＝真实返工点**（GD-1 前端架构师回源；不在 CONTRACT 叙述里）：`check.sh:36` `seq 1 19`（s5-* 锚点）、`:39` `seq 1 13`（p* 屏）、`:81` `len(blocks)==12 and set(anchors)=={20}`（侧栏 12 块逐字一致×每块 20 链接）。任何新屏/新路由/新节必须同批改这三处，否则 build+check 在隐蔽位置变红。
7. **§5/屏编号只追加、永不改号**：新节用 `s5-20`、新屏用 `p14`——`01-topbar-nav.html` doc-nav 逐条硬列 `#s5-1..19`/`#p1..13`，check.sh 第 6/11 项是「目录锚点不得断链」「目录顺序＝文档顺序」两道闸；改号会同时打断本 change 的 §5 交叉引用（D-2 引 §5.14、D-6 引 §5.15/§5.16）。**编号口径勘误（已全量落实）**：清理证据＝§5.15（锚点 s5-16）、报告中心＝§5.16（s5-17）、血缘手册＝§5.14（s5-15）；§5.12/§5.13 实为分诊/变更与发布。
8. **16×8 权限矩阵三处逐字一致没有任何机器校验**（check.sh 只查侧栏一致与锚点完整）：新增模块行只能人工逐格改三处＝静默漂移面。→ 登记为 **2-design 之前的一条工程效能待办**（给 check.sh 补矩阵格一致性检查；具体改法归设计，本 change 不越界）。
9. **新增路由的四处连锁成本**（选路门前置账，GD-1）：① CONTRACT §3 19→20；② 侧栏 blocks/anchor 计数同变（改一条路由＝改全部屏）；③ §3 图标表 sprite id 登记（现有 18 id 无 plug/harness；可复用 `i-cpu`/`i-flow`，或 template-sprite.html 加 symbol——后者不破零依赖红线）；④ 若登记为新模块则 §7/§5.0/p12 三处 +1 行（16×8→17×8）。**低成本替代优先评估**：p9/p12 内加页签承载（零导航、零矩阵、零 sprite 变更；D-3 采 p9 页签退路、D-5 采 p12 第 4 页签）。
10. **⑤ 呈现纪律（GD-1 高级产品经理横切一条）**：晋级清单里凡「没有」必须写成「**哪一层没有＋已有相邻写法在哪（path:line）**」——只核对字面量不核对概念（expires_at vs 复核到期、superseded vs p8/p9 版本行、双时态 vs valid_from/valid_until、绑定 vs p5 ContextBinding、能力状态 vs p9:3341 表）会让用户把「改一列/补一个词」读成「从零建能力」，工作量与优先级被误判（LESSONS L-003 口径，反向同样适用）。
11. **⑤ 题面引用口径（GD-3 高级产品经理横切第二条）**：进 ⑤ 题面与下游工件的 path:line **必须是可编辑真源**（`_build/` 片段、`.md` 行号）；`PRODUCT-DESIGN.html` 是 `build.sh` 生成物，其行号重跑即漂，**只作辅证**。对位替换（产品票已核，D-1/D-2/D-4 与 GAPS 内生成的引用按此读）：`get_lineage`→`_build/sec3.html:482`；`analyze_impact`→`_build/sec3.html:510`；p9 工具/Connector 表→`_build/protoC.html:90`；p8 复核到期列→`_build/protoB.html:486`；§5 `review_due` 不变量→`_build/sec4.html:127`。

## 2. 下游槽位（待 ⑤ 人审晋级后逐条填入，当前全空）

| 槽位 | 拟去向 | 状态 |
|---|---|---|
| （晋级项将按「1-requirement 增量 / 2a-ui-design / backlog 映射 / 平台议题补录 / 留档观察」分类填入） | — | 空置 |

## 3. 跨议题依赖备忘（登记事实，不构成结论；GD-1 锚点修正后）

- D-2 ↔ D-4：p8「最近被哪些 Run 消费」两列依赖 `get_lineage` 消费闭包层先行（初稿「血缘反查端点」措辞已按现状收敛——来源/影响闭包已有，缺的是消费层）。
- D-3 ↔ backlog：界面需求来源＝**D3 召回解释与调试台（P1）**＋A5 配置来源解释（P2）；**B13（P0）是实现前置**（召回只允许一条被解释的路径），不是屏的需求来源（GD-1 高级产品经理改锚；本会话 `sed -n '70p;99p;102p'` 复核属实）。依赖：D-3 注入跳转链接指向 p5，运行数据口径与 D-2 消费闭包共用 `get_lineage`。
- D-5 ↔ backlog C4/C5（P0 承接件）与 **D1 Memory Quality Eval（P0，`:97`＋`:189` 优先序第 6 条）**：主体→资源集清单是金集授权用例输入。初稿「D6 金集」错锚已废（`:102` D6＝显式记忆 CRUD P2，挂它会排期写反，GD-1）。
- D-6 全域：不改 9 步协议编号，只加语义补录，避免与 platform 议题 10 已确认文本冲突；计数到零＝进清理候选走第 5 步处置计划，不自动物理删（GD-1 领域专家限定）。
